# Webserver
import flask
# Data analysis library
import pandas
# Connects to webservers and downloads html
import requests
# Allows to parse html and extract data
from bs4 import BeautifulSoup

from app import run_page

# Make sure to install packages at first

run_page()
